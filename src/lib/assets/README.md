# lib/assets
Képhely-rendszer (7. vasszabály): `getSlotImage(slot)`, `getSlotImages(slot, n)`, `assertLicensedForProduction()`.
A forrás: `src/content/brand/assets.manifest.json`. A komponensek soha nem hivatkoznak fájlnévre.
