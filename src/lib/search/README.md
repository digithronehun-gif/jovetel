# lib/search
`SearchProvider` interfész és a `PostgresSearch` implementáció (FTS `hu_unaccent` + `pg_trgm`), facetek, rendezés,
`candidatesForWizard()`. A súlyok konfigurációs fájlban; **a jutalék nem szerepelhet a rangsorban** (3. vasszabály).
