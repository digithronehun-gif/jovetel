# lib/auth
Supabase Auth (`@supabase/ssr`), `requireUser()`, `requireAdmin()` (szerepkör + MFA `aal2`).
A jogosultságot a route handler / server action **és** az adatréteg is ellenőrzi (4. vasszabály).
