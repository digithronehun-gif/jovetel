# components/app
Domain komponensek. **Ár csak a `PriceBlock` modulból** jelenhet meg; minden webshopba vivő gomb mellé `Disclosure`;
AI-felületen `AiLabel` + `RayIcon`; a `WhyTag` determinisztikus (nincs rajta RayIcon).
